# Notepad

• personalized notepad mobile application that usesNoSQL as a database with an authentication system that allows notes
to be accessed from any device.
• Unlike conventional notepad applications, it does not save the file in cache memory but instead uses a
database.
• Supports authentication system.
